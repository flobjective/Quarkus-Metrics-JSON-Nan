# Repo to highlight an issue with the json exporter 

Run `./mvnw test`. 
Or run `./mvnw quarkus:dev` and `curl http://localhost:8080/q/metrics -H "Accept:application/json"`.

https://github.com/quarkusio/quarkus/issues/14887
